export type CardData = {
  imageUrl: any;
  category: string;
  title: string;
  placeAndDate: string;
  description?: string;
  ddayBadge?: string;
  statusBadge?: string;
  progressRate?: string;
  estimatedPrice?: string;
};
